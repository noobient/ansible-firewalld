---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches:
      - main
      - master
  pull_request:
    branches: []

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Obtain sources
        uses: actions/checkout@v3
      - name: Build container image
        run: podman build --tag ${{ github.repository_owner }}-lint docker/lint
      - name: Lint sources
        run: >
            podman run --rm -v ${{ github.workspace }}:/repo ${{ github.repository_owner }}-lint /bin/bash -c
            "git config --global --add safe.directory /repo &&
            yamllint --strict . &&
            ansible-lint --strict"
